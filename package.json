{
  "name": "varcal",
  "description": "Módulo de variables calculadas",
  "version": "0.1.18",
  "author": "Codenautas <codenautas@googlegroups.com>",
  "license": "MIT",
  "main": "lib/src/var-cal.js",
  "types": "lib/src/var-cal.d.ts",
  "files": [
    "lib",
    "src/client"
  ],
  "dependencies": {
    "backend-plus": "~0.21.6",
    "expre-parser": "~0.1.10",
    "mini-tools": "~1.0.1",
    "operativos": "~0.1.11",
    "pg-promise-strict": "~0.4.7",
    "pg-triggers": "^0.2.1"
  },
  "devDependencies": {
    "types.d.ts": "^0.1.25",
    "@types/node": "~10.3.1",
    "@types/mocha": "~5.2.1",
    "@types/fs-extra": "^5.0.2",
    "discrepances": "~0.2.2",
    "mocha": "^5.2.0"
  },
  "engines": {
    "node": ">= 8.0.0"
  },
  "scripts": {
    "x-test": "tsc || echo \"continue with errors\" && mocha --reporter spec --bail --check-leaks lib\\test\\*.js",
    "test": "(tsc || echo \"continue with errors\") && mocha --reporter spec --bail --check-leaks lib/test/*.js",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --check-leaks test/",
    "start": "node server/server-varcal.js",
    "prepublish": "(tsc -p tsconfig-client.json || echo \"continue w/error\") && (tsc -p tsconfig-server.json  || echo \"continue w/error\")"
  },
  "qa-control": {
    "package-version": "0.3.0",
    "coverage": 90,
    "run-in": "server",
    "stability": "extending",
    "type": "lib",
    "ecmaVersion": 6
  }
}
