{
  "name": "varcal",
  "description": "Módulo de variables calculadas",
  "version": "0.1.2",
  "author": "Codenautas <codenautas@googlegroups.com>",
  "license": "MIT",
  "main": "lib/server/varcal.js",
  "files": [
    "lib",
    "src/client"
  ],
  "dependencies": {
    "@types/node": "~9.4.7",
    "mini-tools": "~1.0.0",
    "pg-promise-strict": "~0.4.6"
  },
  "devDependencies": {
    "@types/mocha": "~2.2.48",
    "mocha": "~5.0.4",
    "discrepances": "~0.2.2",
    "types.d.ts": "~0.1.0"
  },
  "engines": {
    "node": ">= 8.0.0"
  },
  "scripts": {
    "prepublish": "tsc || echo \"continue w/error\" && tsc -p src/client || echo \"continue w/error\" ",
    "xprepublish": "tsc && tsc -p src/client",
    "test": "tsc || echo \"continue with errors\" && mocha --reporter spec --bail --check-leaks lib/",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --check-leaks test/",
    "start": "node server/server-varcal.js"
  },
  "qa-control": {
    "package-version": "0.3.0",
    "coverage": 90,
    "run-in": "server",
    "stability": "extending",
    "type": "lib",
    "ecmaVersion": 6
  }
}
