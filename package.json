{
  "name": "varcal",
  "description": "Módulo de variables calculadas",
  "version": "0.7.2",
  "author": "Codenautas <codenautas@googlegroups.com>",
  "repository": "codenautas/varcal",
  "license": "MIT",
  "main": "dist/server/app-varcal.js",
  "types": "dist/server/app-varcal.d.ts",
  "files": [
    "dist",
    "install"
  ],
  "dependencies": {
    "expre-parser": "~0.1.22",
    "operativos": "~0.7.4"
  },
  "devDependencies": {
    "@types/fs-extra": "^7.0.0",
    "@types/mocha": "~5.2.7",
    "mocha": "^6.1.4",
    "discrepances": "~0.2.4",
    "types.d.ts": "~0.4.4",
    "mixin-patch": "~0.2.0"
  },
  "engines": {
    "node": ">= 10.0.0"
  },
  "scripts": {
    "x-test": "tsc || echo \"continue with errors\" && mocha --reporter spec --bail --check-leaks tests\\tests\\*.js",
    "test": "(npm run build || echo \"continue with errors\") && mocha --reporter spec --bail --check-leaks tests/tests/*.js",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --check-leaks tests/tests/",
    "prepare": "npm run build",
    "build": "tsc -p tsconfig-client.json && tsc -p tsconfig-server.json && mixin-patch",
    "build-force": "(tsc -p tsconfig-client.json || echo \"continue w/error\") && (tsc -p tsconfig-server.json  || echo \"continue w/error\") && mixin-patch",
    "start": "node dist/server/server-varcal.js"
  },
  "qa-control": {
    "package-version": "0.3.0",
    "coverage": 90,
    "run-in": "server",
    "stability": "extending",
    "type": "lib",
    "ecmaVersion": 6
  }
}
